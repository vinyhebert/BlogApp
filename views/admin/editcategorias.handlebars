{{#each erros}} <!--Erros é referente ao Arrey criado no arquivo admin.js para validação do formulario -->
    <div class="alert alert-danger">{{texto}}</div> <!--texto é referente a cada item do arrey -->
{{else}}

{{/each}}
<h3>Editar Categoria</h3>
<div class="card">
    <div class="card-body">
        <form action="/admin/categoria/edit" method="POST">
        <input type="hidden" name="id" value="{{categoria._id}}">
            <label for="nome">Nome</label>
            <input type="text" id="nome" name="nome" class="form-control" value="{{categoria.nome}}">
            <label for="slug">slug</label>
            <input type="text" id="slug" name="slug" class="form-control" value="{{categoria.slug}}">
            <br>
            <button type="submit" class="btn btn-success">Editar categoria</button>
        </form>
    </div>
</div>